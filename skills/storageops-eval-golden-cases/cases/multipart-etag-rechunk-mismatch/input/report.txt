Migration verification mismatch — object ETag differs after copy

Source (AWS S3):
  s3://prod-archive/backups/2026-05/db.tar
  Size: 104857600 bytes (100 MiB)
  ETag: "9bced0aef6f0b2f4a7f2d3c1e8a4b5c6-2"   (multipart, 2 parts)
  Uploaded by aws s3 cp with default 64 MiB multipart chunk size.

Destination (S3-compatible target, via rclone copy):
  Size: 104857600 bytes (100 MiB)   <-- identical byte count
  ETag: "1f0a2b3c4d5e6f70819a2b3c4d5e6f70-7"  (multipart, 7 parts)
  rclone used --s3-chunk-size 16M (16 MiB parts).

Integrity check we already ran:
  Full-object MD5 (streamed, both sides): 3a1f...c9 == 3a1f...c9  -> IDENTICAL
  Byte-for-byte download diff: no differences.

So the object content is the same, but the multipart ETag is different and our
migration tool flags it as a mismatch. Is the data wrong, or is this expected?
Question: why do the ETags differ if the bytes are identical, and how do we make
the destination ETag match the source?
