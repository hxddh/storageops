# Scripts (v0.1 placeholder)

Planned scripts:
- `notification-config-validator.sh` — Verify S3 notification config + destination permissions
- `event-type-checker.py` — Compare event types in config vs actual upload patterns
