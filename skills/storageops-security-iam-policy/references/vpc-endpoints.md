# VPC Endpoint Policy Diagnosis

VPC endpoint policies can deny S3 access even when IAM and bucket policies allow it. Inspect endpoint policy, route table, private DNS settings, and source VPC/subnet when failures only occur from private networks.
