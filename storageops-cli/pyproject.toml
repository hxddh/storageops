[build-system]
requires = ["setuptools>=64"]
build-backend = "setuptools.build_meta"

[project]
name = "storageops"
version = "1.0.0"
description = "StorageOps CLI — object storage diagnostic toolkit"
requires-python = ">=3.9"
dependencies = []

[project.scripts]
storageops = "storageops.cli:main"

[tool.setuptools.packages.find]
where = ["."]
