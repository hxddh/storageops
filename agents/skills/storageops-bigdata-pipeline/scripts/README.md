# Scripts (v0.1 placeholder)

Planned scripts:
- `committer-detector.sh` — Parse Spark/Hive config to identify which OutputCommitter is active
- `small-file-analyzer.py` — Analyze S3 directory listing to detect small-file problems
