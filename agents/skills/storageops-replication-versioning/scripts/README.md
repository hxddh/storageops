# Scripts (v0.1 placeholder)

Implementation deferred to storageops-core v0.2+.

Planned scripts:
- `check_replication_status.sh` — Compare source/replica object metadata and ReplicationStatus
- `audit_version_policy.py` — Check versioning and Object Lock configuration
- `find_orphan_delete_markers.py` — Identify delete markers without replicated source objects
